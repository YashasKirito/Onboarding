import "./styles.css";

const Input = () => {
  return (
    <div className="input">
      <label htmlFor="input">Full Name</label>
      <input type="text" name="text-input" />
    </div>
  );
};

export default Input;
